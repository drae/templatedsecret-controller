apiVersion: v1
kind: Service
metadata:
  labels:
    app: templatedsecret-controller
  name: templatedsecret-controller-metrics
  namespace: templated-secret
spec:
  ports:
    - name: metrics
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: templatedsecret-controller
